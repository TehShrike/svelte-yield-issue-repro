{
  "name": "svelte-yield-issue-repro",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:main": "svelte compile -f umd -n Main -i main.svelte -o main.js",
    "build:yielder": "svelte compile -f umd -n Yielder -i yielder.svelte -o yielder.js",
    "build": "npm run build:main && npm run build:yielder",
    "test": "npm run build && open ./index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "svelte-cli": "1.3.0"
  }
}
